<h1 class= "text-center">Kategorie</h1>
<p class="text-center">
  <button class="btn btn-primary"><%= link_to "Dodaj nową kategorię",new_category_path, class: "text-center text-white text-decoration-none"%></button>
</p>

<div class="flickr_pagination">
  <%= will_paginate @categories, :container => false %>
</div>
<%= render 'categories' %>
<div class="flickr_pagination">
  <%= will_paginate @categories, :container => false %>
</div>
