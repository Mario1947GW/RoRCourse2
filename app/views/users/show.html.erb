<h2 class="text-center">Profil użytkownika: <%= @user.username%></h2>
<% if logged_in? && current_user.username == @user.username%>
  <div class="text-center mt-4">
    <%= link_to "Edytuj profil", edit_user_path(@user), class: "btn btn-outline-info" %>
  </div>
<% end%>
<h3 class="text-center mt-4 mb-4">Artykuły użytkownika</h3>  

<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div>
<%= render 'articles/articles' %>
<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div>